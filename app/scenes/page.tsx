export default function SceneList() {
    return <div>
        <div>
            Scene List
        </div>

        <div>
            Filters :
            - which characters appear
            - voice-over? (voice-actor / narrateur / voix-off) --&gt; boolean first, then MAYBE? a more detailed search.
            - where?
            - what time?
            - scene duration / length
            - dialogues present?
        </div>
    </div>
}