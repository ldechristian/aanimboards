export default function CharacterList() {
    return <div>
        <div>
            Character List
        </div>
        <div>
            Filters :
            - per family
            - per personalities (ex: &gt;50% happy)
            - per name / last name
            - per render type (2D vs 3D)
            - etc... (and much more)
        </div>
    </div>
}